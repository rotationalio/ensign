function SetupTenant() {
  return (
    <section>
      <p>Add image</p>
      <h2>Set up Your Tenant (required)</h2>
      <p>
        A tenant is a collection of settings. The tenant is your locus of control when setting up
        projects and topics.
      </p>
      <button>Set Up</button>
    </section>
  );
}

export default SetupTenant;
